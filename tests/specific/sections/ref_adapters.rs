// use derive_optional::Optional;

// #[derive(Optional, Debug, PartialEq, Eq, Clone, Copy)]
// enum TestType {
//     Something(usize),
//     Nothing,
// }
// use TestType::*;

// as_ref
// only for generic

// as_mut
// only for generic

// as_pin_ref
// only for generic

// as_pin_mut
// only for generic
